# Example environment variables for local development
# Copy to .env and adjust values before running the API locally

DATABASE_URL=postgres://dgz:dgzpass@localhost:5432/cadastre_db
# Optional: change host/port for uvicorn
HOST=127.0.0.1
PORT=8000
